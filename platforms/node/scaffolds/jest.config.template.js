/**
 * Jest config for a Node.js / TypeScript service (ts-jest).
 * Generated by /devkit:cover --setup. Tweak to taste.
 *
 * Tests are centralized under tests/unit/ (NOT co-located with source).
 */
module.exports = {
  preset: 'ts-jest',
  testEnvironment: 'node',

  // Centralized test layout.
  roots: ['<rootDir>/tests', '<rootDir>/src'],
  testMatch: ['**/tests/unit/**/*.test.ts'],

  // reflect-metadata + any logger/env silencing lives here.
  setupFiles: ['<rootDir>/tests/setup.ts'],

  clearMocks: true,

  // Only emitted when tsconfig.json declares compilerOptions.paths.
  // Tests themselves use relative imports, but production code under test may
  // rely on path aliases — keep these in sync with tsconfig.
  moduleNameMapper: {
    {{ moduleNameMapper }}
  },

  // ts-jest reads tsconfig.json by default. Point at a test-specific tsconfig
  // if decorator / type settings differ for tests.
  transform: {
    '^.+\\.ts$': ['ts-jest', { tsconfig: 'tsconfig.json' }],
  },

  collectCoverageFrom: [
    'src/**/*.ts',
    '!src/**/*.d.ts',
    '!src/**/*.types.ts',
    '!src/**/*.enum.ts',
    '!src/**/index.ts',
  ],
  coverageDirectory: '<rootDir>/coverage',
};
