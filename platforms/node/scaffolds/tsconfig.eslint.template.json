{
  "//": "Generated by /devkit:cover --setup when the repo uses type-aware eslint and its build tsconfig excludes *.test.ts. Lets eslint type-lint tests/ without polluting the build. Adjust includes to match the host tsconfig.",
  "extends": "./tsconfig.json",
  "include": ["src/**/*", "config/**/*", "scripts/**/*", "workers/**/*", "tests/**/*"],
  "exclude": ["node_modules", "build", "dist"]
}
