/**
 * Mock for react-native-reanimated.
 *
 * Copies into <package>/__mocks__/react-native-reanimated.js.
 *
 * Uses the library's official mock. Tests that need richer reanimated
 * behavior can override per-test with jest.mock('react-native-reanimated', ...).
 */
module.exports = require('react-native-reanimated/mock');
