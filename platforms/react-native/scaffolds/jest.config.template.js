/**
 * jest.config.js — generated by /devkit:cover --setup
 *
 * Generated from devkit/platforms/react-native/scaffolds/jest.config.template.js.
 * Safe to edit by hand; re-running --setup will detect existing config and
 * only fill in missing keys (won't clobber).
 *
 * Placeholders the command resolves:
 *   {{ moduleNameMapper }}  — auto-derived from babel.config.js aliases in the package
 *   {{ packageRootDir }}    — relative path back to root from the package
 */
module.exports = {
  preset: 'react-native',
  setupFiles: ['<rootDir>/src/__tests__/setup.ts'],
  setupFilesAfterEach: ['@testing-library/jest-native/extend-expect'],

  // Mirrors babel.config.js aliases — auto-generated.
  moduleNameMapper: {
    {{ moduleNameMapper }}
    // test-only aliases
    '^@test-utils/(.*)$': '<rootDir>/src/__tests__/utils/$1',
    '^@fixtures/(.*)$': '<rootDir>/src/__tests__/fixtures/$1',
  },

  transformIgnorePatterns: [
    'node_modules/(?!(' +
      '@react-native|react-native|' +
      '@react-navigation|' +
      '@gorhom/.*|' +
      '@reduxjs/toolkit|immer|reselect|react-redux|' +
      '@practo/self-serve|' +
      '@react-native-async-storage|' +
      'react-native-(reanimated|gesture-handler|safe-area-context|screens|fast-image|svg|linear-gradient|shimmer-placeholder|permissions|document-picker|image-picker|calendars|blob-util|create-thumbnail|pdf|video|video-duration|maps|geocoding|geolocation-service)' +
    ')/)',
  ],

  testPathIgnorePatterns: [
    '/node_modules/',
    '/src/__tests__/utils/',
    '/src/__tests__/fixtures/',
    '/src/__tests__/setup\\.ts$',
  ],

  collectCoverageFrom: [
    'src/**/*.{ts,tsx}',
    '!src/**/*.d.ts',
    '!src/**/__tests__/**',
    '!src/**/__mocks__/**',
    '!src/**/*.mock.ts',
  ],
};
