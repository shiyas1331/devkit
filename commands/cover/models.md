---
description: (android) Generate unit tests for response models with parsing/computed logic in a module. One test file per source file under src/test/java/. Skips the picker.
argument-hint: <module-path>
model: opus
---

# /devkit:cover:models — android model batch

Equivalent to `/devkit:cover <path> --batch models`. Skips the picker. Android platform only.

Set `CLASSIFICATION=model`, then read `commands/cover/android-batch.md` and
follow it verbatim with that classification.
